import ControlledCarousel from "./homeCarousel";
import Carousel from "./homeCarousel";
function Home(){
    return (
      <div>
        <h1>POC Barcoding</h1>
        <ControlledCarousel / >
      </div>
    )
  }

export default Home;